version: '3.8'

services:
  app:
    build: .
    container_name: node-docker-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./:/app
      - /app/node_modules